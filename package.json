{
  "name": "epubifyer",
  "module": "src/index.ts",
  "version": "0.0.1",
  "description": "A tool to convert EPUB files to a more web-friendly format.",
  "author": "The Ducktor",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --minify",
    "test": "bun test"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "jszip": "^3.10.1"
  }
}
